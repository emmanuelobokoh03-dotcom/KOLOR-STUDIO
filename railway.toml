[build]
builder = "NIXPACKS"

[build.nixpacksPlan]
phases.setup = ["..."]
phases.install = ["cd backend && npm ci"]
phases.build = ["cd backend && npx prisma generate && npm run build"]

[deploy]
startCommand = "cd backend && npm start"

[env]
NODE_ENV = "production"
